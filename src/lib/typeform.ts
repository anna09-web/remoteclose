// Typeform integration config.
//
// Once the RemoteClose Typeform account is set up, replace the placeholder
// below with either:
//   1. The Typeform "Create Response" API endpoint, if submitting data
//      programmatically (requires a backend proxy to keep the API key
//      secret — do NOT call the Typeform API directly from the browser
//      with a secret key), or
//   2. The public Typeform form URL, if embedding/redirecting to a
//      hosted Typeform instead of using the custom form below.
//
// TODO: connect Typeform here.
export const TYPEFORM_ENDPOINT = "";

// Set to true to redirect/embed the hosted Typeform instead of rendering
// the custom ApplicationForm component.
export const USE_TYPEFORM_EMBED = false;
